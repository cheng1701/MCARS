def add_request_to_context(request):
    """Adds the request object to the context for use in email templates"""
    return {'request': request}
def add_request_to_context(request):
    """Adds the request object to the context for use in email templates"""
    return {'request': request}